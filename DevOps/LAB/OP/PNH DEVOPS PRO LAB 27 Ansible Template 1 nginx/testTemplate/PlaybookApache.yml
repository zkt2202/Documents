- hosts: Apache
  become: yes
  tasks:
    - name: "apt-get update"
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: "install Apache"
      apt:
        name: ["apache2"]
        state: latest
